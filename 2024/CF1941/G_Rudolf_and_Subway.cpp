#include <bits/stdc++.h>

using namespace std;

const int N=2e5+10;

int t,n,m;

void solve(){
    cin>>n>>m;
    vector<set<int> > c(n+1); 
    for(int i=1;i<=m;i++){
        int u,v,cl;
        cin>>u>>v>>cl;
        
    }
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    cin>>t;
    while(t--){
        solve();
    }
    return 0;
}