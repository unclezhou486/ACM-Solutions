#include <bits/stdc++.h>

using namespace std;

void solve(){
	int n;
	string str;
	cin>>n>>str;
	cout<<str<<' '<<"miao";
}

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	int t=1;
	// cin>>t;
	while(t--){
		solve();
	}
	return 0;
}